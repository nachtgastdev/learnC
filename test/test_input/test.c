#include "head.h"

int main() {
	puts("Test!!");
	{
	//int ch;
	//ch = getchar();  //Получаем код символа (ASCII)и кладем в переменную
	//fflush(stdin); // Очищает поток ввода
	//getchar();	   // Останавливаем программу до нажатия Enter
	//putchar(120);  // Вывод символа по коду ASCII
	//printf("%d\n", ch);
	//if (ch == 100) {
		//puts("It is work");
	//} else {
		//puts("It is not work");
	//}
	
	// Выход из цикла!!
	//int ext;
	//do {
		//puts("press 'x' for exit");
		//ext = getchar();
		//getchar();
	//} while (ext != 120);
}
	{
	// рандомное число от 0 до 100
	//int x = rand() % 101;
	//printf("%d\n", x);
}
	{
	// считываем символ
	//char ch;
	//scanf("%c", &ch);
	//printf("%c", ch);
}
	{
	//char array[] = {};
	//fgets(array, 5, stdin);
	//fflush(stdin);
	//puts(array);
}
	char str;
	str = read_string_and_allocate ();
	puts(str);
	return 0;
}
